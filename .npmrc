# Force npm as package manager (no pnpm or yarn)
engine-strict=true

# optional=false (REMOVED - this was blocking @next/swc packages)

# Use legacy peer deps to avoid conflicts
legacy-peer-deps=false

# Strict SSL
strict-ssl=true

# Save exact versions
save-exact=true

# Progress bar
progress=true

# Disable fund messages
fund=false
